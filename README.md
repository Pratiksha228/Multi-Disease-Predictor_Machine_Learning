# Multi-Disease-Predictor_Machine_Learning
This webapp was developed using Flask Web Framework and was deployed on Local server. The models used to predict the diseases were trained on large Datasets.
